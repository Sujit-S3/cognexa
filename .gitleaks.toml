title = "Cognexa Gitleaks Configuration"

[allowlist]
description = "Allow only the exact non-secret value shared by the CI and test environment"
regexes = [
  '''^test-secret-key-that-is-at-least-32-characters-long$'''
]
