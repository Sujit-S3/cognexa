apiVersion: v1
kind: Service
metadata:
  name: cognexa-web
  labels:
    app.kubernetes.io/name: cognexa-web
    app.kubernetes.io/component: web
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: cognexa-web
  ports:
    - name: http
      port: 8080
      targetPort: http
